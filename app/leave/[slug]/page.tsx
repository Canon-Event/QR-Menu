import{notFound}from'next/navigation';import{createClient}from'@supabase/supabase-js';import LeaveApplicationForm from '@/components/employees/LeaveApplicationForm'
export const dynamic='force-dynamic';export default async function CommonLeavePage({params}:{params: Promise<{slug:string}>}){if(!/^[a-z0-9-]{3,50}$/.test((await params).slug)||!process.env.SUPABASE_SERVICE_ROLE_KEY)notFound();const admin=createClient(process.env.NEXT_PUBLIC_SUPABASE_URL!,process.env.SUPABASE_SERVICE_ROLE_KEY!);const{data:restaurant}=await admin.from('restaurants').select('name,logo_url').eq('slug',(await params).slug).eq('is_active',true).maybeSingle();if(!restaurant)notFound();return <main className="leave-portal-page"><section><header>{restaurant.logo_url?<img src={restaurant.logo_url} alt=""/>:<span>♧</span>}<div><small>{restaurant.name}</small><h1>Employee leave request</h1><p>Enter your employee details and requested leave dates.</p></div></header><LeaveApplicationForm slug={(await params).slug}/><footer>Your request will be sent privately to the restaurant owner.</footer></section></main>}
